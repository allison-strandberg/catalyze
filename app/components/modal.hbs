  <PaperDialog
    @class="dialog"
    @onClose={{@onClose}}
    @parent=".floor"
    @clickOutsideToClose={{true}}
    @fullscreen={{true}}
    @escapeToClose={{true}}
  >
    <div class="parchment {{@classNames}}">
      <button class="close-button" {{on "click" @onClose}}>X</button>
      {{yield}} 
    </div>
  </PaperDialog>
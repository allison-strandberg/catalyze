  <PaperDialog
    @class="dialog"
    @onClose={{@onClose}}
    @parent=".floor"
    @clickOutsideToClose={{true}}
    @fullscreen={{true}}
    @escapeToClose={{true}}
  >
    <div class="dialog-content {{@classNames}}">
      <button class="close-button" {{on "click" @onClose}}>X</button>
      {{yield}} 
    </div>
  </PaperDialog>